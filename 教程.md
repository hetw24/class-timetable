## 🚀 部署指南 (Deployment Guide)

通过 Cloudflare 网页控制台，只需鼠标点击即可轻松完成部署：

---

### 📋 0. 前置准备

在开始之前，请准备好以下两项配置：

1. ☁️ **Cloudflare 账号**：注册并登录 [Cloudflare 控制台](https://dash.cloudflare.com/)。
2. 🤖 **飞书群机器人**：
* 打开飞书群聊 ➔ 点击右上角 `设置` ➔ `群机器人` ➔ `添加机器人` ➔ 选择 `自定义机器人`。
* 设置机器人名称，复制生成的 **Webhook 链接** 备用。



---

### 🛠️ 网页端部署步骤

#### 1. 🗄️ 创建课程数据库 (KV)

1. 登录 Cloudflare 控制台，在左侧导航栏选择 **Workers & Pages** ➔ 点击 **KV**。
2. 点击 **Create a namespace**，名称填写 `timetable`，点击 **Add**。
3. 点击刚创建的 `timetable` 进入管理页面，在 **KV Pairs** 标签页下点击 **Add entry**：
* **Key**: `freshman-1`
* **Value**: 粘贴你的课程 JSON 数组（数据格式参考下文📦 数据格式说明）



---

#### 2. ⚡ 创建并配置 Worker

1. 返回控制台左侧菜单，选择 **Workers & Pages** ➔ **Overview**，点击 **Create Application** ➔ **Create Worker**。
2. 输入 Worker 名称（如 `feishu-course-reminder`），点击 **Deploy**。
3. 点击 **Edit Code**，将本项目中的 `index.js` 代码全选粘贴并**覆盖**原有代码。
4. **修改头部基础配置**：
* `FEISHU_WEBHOOK_URL`：替换为你的飞书 Webhook 链接。
* `PERIOD_CONFIG`：按需调整学校的开课时间。


5. 点击右上角 **Save and deploy** 保存并部署。

---

#### 3. 🔗 绑定 KV 数据库

1. 返回该 Worker 的管理页面，选择 **Settings** ➔ **Variables**。
2. 找到 **KV Namespace Bindings**，点击 **Add binding**：
* **Variable name**: `TIMETABLE_DB`
* **KV namespace**: 选择第一步创建的 `timetable`。


3. 点击 **Save and deploy**。

---

#### 4. ⏰ 配置 Cron Trigger 定时任务

1. 在 Worker 管理页面选择 **Triggers** 选项卡。
2. 找到 **Cron Triggers** 区域，点击 **Add Trigger**。
3. 填入 Cron 表达式 `* * * * *`（代表每分钟自动巡检，精准匹配课前提醒点），点击 **Add Trigger** 保存。

---

### 🔍 部署后验证

访问 Cloudflare 分配给你的 Worker 专属域名，验证各接口功能：

* 🤖 **测试推送**：访问 `/test-notify`
* 触发发送一条默认测试消息至飞书群，验证 Webhook 连通性。


* 🩺 **接口诊断**：访问 `/debug`
* 查看当前北京时间、自适应计算的提醒时间表以及 KV 课程数据读取状态。



---

### 📦 数据格式说明

存入 KV（Key 为 `freshman-1`）的课程 JSON 格式示例如下：

```json
[
  {
    "id": "1784602834435",
    "name": "高等数学",
    "day": 1,          // 🗓️ 星期几 (1-7，1 代表周一)
    "period": 2,       // ⏰ 对应上课节次 (如 2 代表第 3-4 节)
    "weeks": "1-16",   // 🗓️ 上课周次
    "room": "教二 201",  // 📍 上课教室
    "teacher": "张老师"  // 👨‍🏫 任课教师
  }
]

```
